# ボタン画像の置き場所

ここに Allow ボタンのスクリーンショット (PNG 推奨) を置いてください。
`*.png / *.jpg / *.jpeg / *.bmp` はすべてテンプレートとして読み込まれます。

- ボタン**だけ**を切り抜く(周囲の余白・背景は最小限に)
- 実際に使う画面と同じ表示倍率・テーマで撮る
- 見た目が変わる状態(ホバー時、ライト/ダークテーマ等)があれば、それぞれ別ファイルで追加
